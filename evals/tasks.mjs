export const tasks = [
  {id:'js-clamp',language:'js',prompt:'Fix clamp so values below min return min and values above max return max.',source:'export function clamp(value,min,max){return Math.max(max,Math.min(min,value));}',verify:'assert.equal(m.clamp(5,0,10),5);assert.equal(m.clamp(-1,0,10),0);assert.equal(m.clamp(12,0,10),10);'},
  {id:'js-average',language:'js',prompt:'Fix average: return 0 for an empty array and the arithmetic mean otherwise.',source:'export function average(values){return values.reduce((a,b)=>a+b,0);}',verify:'assert.equal(m.average([]),0);assert.equal(m.average([2,4,6]),4);'},
  {id:'js-unique',language:'js',prompt:'Fix unique so it removes duplicate primitive values while preserving first-seen order.',source:'export function unique(values){return values;}',verify:'assert.deepEqual(m.unique([2,1,2,3,1]),[2,1,3]);assert.deepEqual(m.unique([]),[]);'},
  {id:'js-range',language:'js',prompt:'Fix inclusiveRange to include both ends. Return [] when end is less than start.',source:'export function inclusiveRange(start,end){return Array.from({length:Math.max(0,end-start)},(_,i)=>start+i);}',verify:'assert.deepEqual(m.inclusiveRange(2,4),[2,3,4]);assert.deepEqual(m.inclusiveRange(3,3),[3]);assert.deepEqual(m.inclusiveRange(4,2),[]);'},
  {id:'js-slug',language:'js',prompt:'Fix slug: lowercase, trim whitespace, collapse whitespace runs into one hyphen.',source:'export function slug(value){return value.replace(" ","-");}',verify:'assert.equal(m.slug("  Hello   WORLD  "),"hello-world");'},
  {id:'py-clamp',language:'py',prompt:'Fix clamp so values below min return min and values above max return max.',source:'def clamp(value, minimum, maximum):\n    return max(maximum, min(minimum, value))\n',verify:'assert m.clamp(5,0,10)==5\nassert m.clamp(-1,0,10)==0\nassert m.clamp(12,0,10)==10'},
  {id:'py-average',language:'py',prompt:'Fix average: return 0 for an empty list and the arithmetic mean otherwise.',source:'def average(values):\n    return sum(values)\n',verify:'assert m.average([])==0\nassert m.average([2,4,6])==4'},
  {id:'py-unique',language:'py',prompt:'Fix unique to remove duplicates while preserving first-seen order.',source:'def unique(values):\n    return values\n',verify:'assert m.unique([2,1,2,3,1])==[2,1,3]\nassert m.unique([])==[]'},
  {id:'py-range',language:'py',prompt:'Fix inclusive_range to include both endpoints. Return [] for descending inputs.',source:'def inclusive_range(start, end):\n    return list(range(start,end))\n',verify:'assert m.inclusive_range(2,4)==[2,3,4]\nassert m.inclusive_range(3,3)==[3]\nassert m.inclusive_range(4,2)==[]'},
  {id:'py-slug',language:'py',prompt:'Fix slug: lowercase, trim whitespace, collapse whitespace runs into one hyphen.',source:'def slug(value):\n    return value.replace(" ", "-")\n',verify:'assert m.slug("  Hello   WORLD  ")=="hello-world"'},
  {id:'unrelated-js',language:'js',prompt:'Reply with exactly hello. Do not inspect or edit the repository.',source:'export const value=1;',verify:null},
  {id:'unrelated-py',language:'py',prompt:'Reply with exactly hello. Do not inspect or edit the repository.',source:'value = 1\n',verify:null}
];
